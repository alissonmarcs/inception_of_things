#!/usr/bin/bash

# call install_dependencies.sh
# create argocd and dev namespace
# check if bonus variable is set to true, if true, call script to deploy gitlab on vm and then deploy applications
# search for needed steps.
